using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ViewInit : MonoBehaviour
{

    void Start()
    {
        
    }


    void Update()
    {
        
    }

    private void ButtonInit()
    {
        if(Input.GetMouseButtonDown(0))
        {   
            //float eX = Input.mousePosition.x/centerX - 1;
            //float eY = Input.mousePosition.y/centerY - 1;
            
            //float angleCam = 60;
            //if(state=="solarsystem")
            //{heightCam = transform.position.y;}
            //if(state=="galaxy")
            //{heightCam = transform.position.y-corGalaxy.y;}
            //float corX = heightCam/kHeight*eX;
            //float corZ = heightCam/kWidth*eY;
            //Debug.Log("X:"+corX);
            //Debug.Log("Z:"+corZ);
        }
    }
}
